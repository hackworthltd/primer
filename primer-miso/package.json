{
  "name": "primer-miso",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rspack build --mode=development --entry=./frontend/index.js",
    "build:prod": "rspack build --mode=production --entry=./frontend/index.js",
    "watch": "rspack watch --entry=./frontend/index.js",
    "serve": "rspack serve --entry=./frontend/index.js"
  },
  "devDependencies": {
    "@rspack/cli": "^1.6.1",
    "@rspack/core": "^1.6.1",
    "css-loader": "^6.10.0",
    "html-rspack-plugin": "^6.1.3"
  }
}
